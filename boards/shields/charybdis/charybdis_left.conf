# Enable general pointer (mouse) support
CONFIG_ZMK_POINTING=y   # turns on ZMK’s pointing device functionality :contentReference[oaicite:1]{index=1}

# (Optional) If you plan to use “mouse keys” (move/scroll via keypress),
# also enable the mouse feature
CONFIG_ZMK_MOUSE=y
